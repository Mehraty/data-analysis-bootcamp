1.	**File name:** `even_index_printer.py`
   
**توضیح فارسی:** کد یک ورودی از کاربر می‌گیرد و کاراکترهای زوج (با ایندکس 0، 2، 4 و ...) را چاپ می‌کند.
**English description:** Takes user input and prints characters at even indices (0, 2, 4, ...).

3.	**File name:** `rectangle_area_calculator.py`
   
**توضیح فارسی:** طول و عرض مستطیل را از کاربر گرفته و مساحت آن را محاسبه و چاپ می‌کند.
**English description:** Takes length and width of a rectangle from user and calculates/prints its area.

5.	**File name:** `name_joiner_with_underscore.py`
   
**توضیح فارسی:** نام کامل کاربر را گرفته، کلمات آن را جدا کرده و با خط زیر (_) به هم می‌چسباند و چاپ می‌کند.
**English description:** Takes user's full name, splits it into words, joins them with underscores (_), and prints the result.

7.	**File name:** `odd_numbers_until_threshold.py`
   
**توضیح فارسی:** اعداد فرد را از ۱ تا ۱۰ چاپ می‌کند و اعداد زوج را رد می‌کند.
**English description:** Prints odd numbers from 1 to 10, skipping even numbers using continue.

9.	**File name:** `guess_the_number_game.py`
    
**توضیح فارسی:** یک بازی حدس عدد که عددی تصادفی بین ۱ تا ۱۰۰ انتخاب می‌کند و کاربر باید آن را حدس بزند.
**English description:** A number guessing game that picks a random number between 1 and 100 for the user to guess.

11.	**File name:** `alternating_chunk_sum.py`
    
**توضیح فارسی:** اعداد ورودی را به گروه‌های سه‌تایی تقسیم می‌کند، مجموع هر گروه را محاسبه می‌کند و به‌صورت یک‌درمیان جمع و منها می‌کند.
**English description:** Splits input numbers into groups of three, calculates each group's sum, then alternately adds and subtracts them.

13.	**File name:** `string_reversal_methods.py`
    
**توضیح فارسی:** شش روش مختلف برای معکوس کردن یک رشته در پایتون — همه خروجی یکسان دارند.
**English description:** Six different ways to reverse a string in Python — all produce the same output.
15.	**File name:** `name_age_printer.py`

**توضیح فارسی:** نام و سن کاربر را می‌گیرد و با f-string نمایش می‌دهد.
**English description:** Takes user's name and age, then displays them using an f-string.

16.	**File name:** `word_replacer.py`
    
**توضیح فارسی:** یک کلمه از کاربر می‌گیرد و جایگزین آن را هم می‌پرسد، سپس نتیجه را نمایش می‌دهد.
**English description:** Takes a word and its replacement from the user, then shows the result.

18.	**File name:** `student_grades.py`
    
**توضیح فارسی:** نمرات دانش‌آموزان را به حروف (A تا F) تبدیل می‌کند و میانگین را محاسبه می‌کند.
**English description:** Converts student scores to letter grades (A–F) and calculates the average.

20.	**File name:** `tkinter_hello.py`
    
**توضیح فارسی:** یک پنجره ساده با Tkinter که یک دکمه دارد و با کلیک پیام "hello bro!" را نمایش می‌دهد.
**English description:** A simple Tkinter window with a button that shows "hello bro!" on click.

22.	**File name:** `count_and_unique.py`
    
**توضیح فارسی:** تعداد تکرار هر عدد را در یک دیکشنری ذخیره می‌کند و لیست اعداد یکتا را با `set` استخراج می‌کند.
**English description:** Counts occurrences of each number into a dictionary and extracts unique numbers using `set`.

24.	**File name:** `count_and_unique_user_input.py`
    
**توضیح فارسی:** ورودی کاربر را می‌گیرد (اعداد با فاصله)، تعداد تکرار هر عدد را محاسبه می‌کند و اعداد یکتا را نمایش می‌دهد. خطای ورودی غیرعددی را هم مدیریت می‌کند.
**English description:** Takes user input (space-separated numbers), counts occurrences, and shows unique numbers. Handles non-numeric input errors.

26.	**File name:** `meters_to_cm.py`
    
**توضیح فارسی:** یک متر رو از کاربر میگیره و به سانتی‌متر تبدیل میکنه. خیلی ساده و مستقیم.
**English description:** Takes meters from user and converts to centimeters. Simple and straightforward.

28.	**File name:** `celsius_to_fahrenheit.py`
    
**توضیح فارسی:** دمای سلسیوس رو از کاربر میگیره و به فارنهایت تبدیل میکنه. فرمول: `(C × 9/5) + 32`
**English description:** Converts Celsius to Fahrenheit using the formula `(C × 9/5) + 32`.


30.	**File name:** `find_max_number.py`
    
**توضیح فارسی:** اعداد رو با فاصله از کاربر میگیره و بزرگترین عدد رو پیدا میکنه. خطای ورودی غیرعددی رو هم هندل میکنه.
**English description:** Takes space-separated numbers from user and finds the maximum. Handles invalid input errors.

32.	**File name:** `calculator_class.py`
    
**توضیح فارسی:** یک ماشین حساب ساده با کلاس `Calculator` که چهار عمل اصلی رو انجام میده. کاربر عملیات و دو عدد رو وارد میکنه و نتیجه رو میبینه. تقسیم بر صفر هم هندل شده.
**English description:** A simple calculator class with four basic operations. User selects operation and enters two numbers. Division by zero is handled.

34.	**File name:** `bmi_calculator_gui.py`
    
**توضیح فارسی:** یک برنامه گرافیکی با Tkinter برای محاسبه BMI. وزن (کیلوگرم) و قد (سانتی‌متر) رو میگیره، BMI رو محاسبه میکنه و در یک پیغام نشون میده. ورودی غیرعددی رو هم هندل میکنه.
**English description:** A Tkinter GUI app that calculates BMI from weight (kg) and height (cm). Shows result in a message box. Handles invalid input.
